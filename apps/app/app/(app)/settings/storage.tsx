import { StubScreen } from '@/ui/StubScreen';
import { t } from '@/i18n';

export default function SettingsStorageScreen() {
  return <StubScreen title={t('settings.storage')} body={t('settings.stub')} />;
}
