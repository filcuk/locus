import { StubScreen } from '@/ui/StubScreen';
import { t } from '@/i18n';

export default function SettingsProfileScreen() {
  return <StubScreen title={t('settings.profile')} body={t('settings.stub')} />;
}
