import { StubScreen } from '@/ui/StubScreen';
import { t } from '@/i18n';

export default function SettingsSecurityScreen() {
  return <StubScreen title={t('settings.security')} body={t('settings.stub')} />;
}
