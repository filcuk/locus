import { StubScreen } from '@/ui/StubScreen';
import { t } from '@/i18n';

export default function SettingsTrashScreen() {
  return <StubScreen title={t('settings.trash')} body={t('settings.stub')} />;
}
