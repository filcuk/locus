import { StubScreen } from '@/ui/StubScreen';
import { t } from '@/i18n';

export default function SettingsTagsScreen() {
  return <StubScreen title={t('settings.tags')} body={t('settings.stub')} />;
}
