import { StubScreen } from '@/ui/StubScreen';
import { t } from '@/i18n';

export default function SettingsInvitesScreen() {
  return <StubScreen title={t('settings.invites')} body={t('settings.stub')} />;
}
