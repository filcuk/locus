import { StubScreen } from '@/ui/StubScreen';
import { t } from '@/i18n';

export default function SettingsNotificationsScreen() {
  return (
    <StubScreen title={t('settings.notifications')} body={t('settings.stub')} />
  );
}
