import { StubScreen } from '@/ui/StubScreen';
import { t } from '@/i18n';

export default function PlaceDetailScreen() {
  return (
    <StubScreen title={t('place.detail.title')} body={t('place.detail.stub')} />
  );
}
