import { StubScreen } from '@/ui/StubScreen';
import { t } from '@/i18n';

export default function CollectionsScreen() {
  return (
    <StubScreen title={t('collections.title')} body={t('collections.empty')} />
  );
}
