packages:
  - apps/*
  - packages/*

allowBuilds:
  esbuild: set this to true or false

# pnpm blocks dependency build scripts unless they are named here. esbuild's
# postinstall only unpacks its platform binary, which Vitest needs to run.
onlyBuiltDependencies:
  - esbuild
