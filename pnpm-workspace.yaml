packages:
  - apps/*
  - packages/*

# pnpm blocks dependency build scripts unless allowed here. esbuild's
# postinstall only unpacks its platform binary, which Vitest needs to run.
allowBuilds:
  esbuild: true

onlyBuiltDependencies:
  - esbuild
